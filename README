Some notes
###########

# In "pageapp.yaml" file, replace the line having '172.17.200.84' 
# with the public IP in for the production version, i.e. the line 
# for www-dev is:
root: http://172.17.200.84/static/page/genomes

# An example way to run this web app
    #cd into utilities directory and run the "start" script.

# "start" calls "pageapp_init.sh", which calls "start_starman.sh".

# The web server will be listening to port 8000. To change, edit the "pageapp_init.sh".

# Session data directory. This location is configurable via pageapp.yaml file.
The sessions (of www-dev) are stored here '/www/tmp/www-dev/session'.

# A crontab such as the one below, can clean up any old (www-dev) session data:
*/30 * * * * find /www/tmp/www-dev/session/ -maxdepth 1 -type d -regex "/www/tmp/www-dev/session/.+" -cmin +120 -exec rm -rf {} \;

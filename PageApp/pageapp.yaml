# rename this file to pageapp.yml and put a ':' after 'name' if
# you want to use YAML like in old versions of Catalyst
name: PageApp

#navigation menu structure is defined here
#currently, we only support one-level (primary) menus
navigation:
    primary:
        - action: index
          title: HOME
        - action: consortium
          title: CONSORTIUM
        - action: data
          title: DATA
        - action: contact
          title : CONTACT
#YAML like in old versions of Catalyst
name: PageApp

#navigation menu structure is defined here
#currently, we only support one-level (primary) menus
navigation:
    primary:
        - action: /
          title: HOME
        - action: /page/consortium
          title: CONSORTIUM
        - action: /data/authenticate/data
          title: DATA
        - action: /data/authenticate/resources
          title : RESOURCES
        - action: /page/contact
          title : CONTACT